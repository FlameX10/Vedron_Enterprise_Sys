# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://parasmp10_db_user:dVDsv0AHLUSGBXXM@cluster0.ziurv99.mongodb.net/?appName=Cluster0

# Secret for signing session cookies (Keep strong and secret)
SESSION_SECRET=your_super_secret_session_key_change_in_production

# Backend Server Port (Default: 5000)
PORT=5000

# Frontend URL for CORS configuration (No trailing slash)
FRONTEND_URL=http://localhost:5173

# Node Environment (development | production)
NODE_ENV=development
